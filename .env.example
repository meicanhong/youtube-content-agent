# Editorial provider: mimo (default) or openai.
EDITORIAL_PROVIDER=mimo

# Default production provider: Xiaomi MiMo V2.5, OpenAI-compatible Chat Completions.
MIMO_API_KEY=
MIMO_MODEL=mimo-v2.5
MIMO_BASE_URL=https://api.xiaomimimo.com/v1

# Optional alternative provider.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini

# Optional. The URL-to-package MVP uses yt-dlp and does not require this key.
# It is reserved for the later Trend Agent.
YOUTUBE_API_KEY=

# Optional explicit paths.
YT_DLP_BIN=yt-dlp
FFMPEG_BIN=ffmpeg
FFPROBE_BIN=ffprobe

# Optional browser cookie import for videos YouTube refuses anonymously.
# Example: chrome, safari, firefox. Never commit exported cookies.
YT_DLP_COOKIES_FROM_BROWSER=
